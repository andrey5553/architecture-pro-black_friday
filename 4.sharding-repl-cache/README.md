# Cпринт 4 (приложение сервиса "Мобильный мир")

## Задание 4. Кеширование

2. Выполняем все шаги из Задания 3, но можно просто не удалять контейнер после Задания 3 и дополнить его шагами:
  ``` docker-compose -f compose.yaml up redis -d ``` - добавляем редис в контейнеры проекта
  ``` docker-compose stop pymongo_api ```  - останавливаем api 
  ``` docker-compose up -d --build pymongo_api ``` - билд и запуск api, дабы подхватить редис
  добавляем Redis и перезапускаем апи проект, для возможности кеширования данных.

3. Проверка работы кеширования данных
  Заполняем список пользователей, используем ручку post http://localhost:8082/helloDoc/users (передаем сведения о пользователе)

  В качестве инструментария проверки работы кеша был использован Postman
  
  3.1 Вызываем ручку get http://localhost:8082/helloDoc/users, как результат наблюдаем TTBF 1.03 секунды
  [первичный вызов](/images/ручка%20users%20без%20наличия%20кеша.png)
  3.2 Повторно вызываем ручку get http://localhost:8082/helloDoc/users, как результат наблюдаем TTBF 14 миллисекунд
  [повторный вызов](/images/ручка%20users%20кеш%20активирован.png)
  3.3 Также можно заглянуть в контейнер редиса в докере и посмотреть на его коллекцию, которая заполняется значением
  на одну минуту и далее очищается [хранилище редиса](/images/заполнение%20кеша%20в%20редис,%20демонстрация%20записи%20.png)

## Вывод: кеширование работает корректно

  
